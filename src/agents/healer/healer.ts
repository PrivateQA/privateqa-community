// MVP: stub
// Objectif futur:
// - intercepter les erreurs Playwright (Timeout, strict mode, etc.)
// - capturer screenshot + DOM
// - demander à un LLM vision (local) de proposer un nouveau locator
// - patcher le .spec.ts ou le "map"
export function healerNotImplemented() {
  throw new Error("Healer non implémenté (MVP).");
}

